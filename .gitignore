# Claude Code / editor
.claude/
.vscode/
.idea/
.DS_Store

# OS
Thumbs.db

# Production secrets (copy .env.production.example -> .env on the server, never commit it)
.env
.env.production

# Design references (large files, not app source)
design/
